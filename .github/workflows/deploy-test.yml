name: Deploy (test)

# Deploy-only workflow for the test environment.
# Called automatically by build-test.yml after a successful build.
# Can also be triggered manually to re-deploy any existing image tag (e.g. rollbacks).
on:
  workflow_call:
    inputs:
      image_tag:
        description: 'Image tag to deploy (e.g. v1.2.3-test)'
        required: true
        type: string
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Image tag to deploy (e.g. v1.2.3-test)'
        required: true
        type: string

permissions: {}
concurrency:
  group: test
  cancel-in-progress: false

jobs:
  deploy-test:
    name: Deploy to test
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    environment: test

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Authenticate to the cluster
        env:
          KUBE_NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}
          KUBE_CLUSTER: ${{ secrets.KUBE_CLUSTER }}
        run: |
          echo "${{ secrets.KUBE_CERT }}" > ca.crt
          kubectl config set-cluster ${KUBE_CLUSTER} --certificate-authority=./ca.crt --server=https://${KUBE_CLUSTER}
          kubectl config set-credentials deploy-user --token=${{ secrets.KUBE_TOKEN }}
          kubectl config set-context ${KUBE_CLUSTER} --cluster=${KUBE_CLUSTER} --user=deploy-user --namespace=${{ secrets.KUBE_NAMESPACE }}
          kubectl config use-context ${KUBE_CLUSTER}

      - name: Clone Helm chart repo
        run: |
          git clone https://github.com/ministryofjustice/track-my-case-cloud-platform.git
          git -C track-my-case-cloud-platform checkout main

      - name: Configure AWS credentials (test ECR)
        uses: aws-actions/configure-aws-credentials@v5
        with:
          role-to-assume: ${{ secrets.TEST_ECR_ROLE_TO_ASSUME }}
          aws-region: ${{ vars.TEST_ECR_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Upgrade the Helm chart
        env:
          ECR_REPO_WEBAPP: ${{ steps.login-ecr.outputs.registry }}/${{ vars.TEST_ECR_REPOSITORY }}
          KUBE_NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}
          NEW_TAG_V: ${{ inputs.image_tag }}
          RELEASE_NAME: ${{ github.event.repository.name }}-test
          SECRETS_CONTEXT: ${{ toJson(secrets) }}
          VARS_CONTEXT: ${{ toJson(vars) }}
        run: |
          combined_json=$(jq -n \
              --argjson secrets_json "$SECRETS_CONTEXT" \
              --argjson vars_json "$VARS_CONTEXT" \
              '$secrets_json + $vars_json')

          custom_variables=""
          for row in $(echo "${combined_json}" | jq -r 'to_entries[] | @base64'); do
            key=$(echo ${row} | base64 --decode | jq -r '.key')
            value=$(echo ${row} | base64 --decode | jq -r '.value')
            if [[ $key == XXX* ]]; then
                custom_key=$(echo $key | sed 's/^XXX_/Secrets.WebApp.Parameters./')
                custom_variables="$custom_variables --set $custom_key=$value"
            fi
          done

          helm upgrade --install --atomic --wait --timeout 5m0s --namespace $KUBE_NAMESPACE $RELEASE_NAME ./track-my-case-cloud-platform/charts/webapp-cp \
          --set Namespace=$KUBE_NAMESPACE \
          --set WebApp.Image.Repository=$ECR_REPO_WEBAPP \
          --set WebApp.Image.Tag=$NEW_TAG_V \
          --set WebApp.Name=$KUBE_NAMESPACE \
          --set securityContext.allowPrivilegeEscalation=false \
          --set securityContext.capabilities.drop=["ALL"] \
          --set securityContext.runAsNonRoot=true \
          --set securityContext.seccompProfile.type="RuntimeDefault" \
          --set Ingress.Enabled=true \
          $custom_variables

          echo "Labeling the service"
          kubectl label service --overwrite=true $KUBE_NAMESPACE app=$KUBE_NAMESPACE

      - name: Extract deployed service values for monitoring
        id: extract-values
        env:
          RELEASE_NAME: ${{ github.event.repository.name }}-test
          KUBE_NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}
        run: |
          echo "Extracting deployed Helm values..."
          SERVICE_NAME=$(helm get values "$RELEASE_NAME" -n "$KUBE_NAMESPACE" -o json | jq -r '.WebApp.Name')
          echo "ServiceName=$SERVICE_NAME"
          echo "service_name=$SERVICE_NAME" >> $GITHUB_OUTPUT

      - name: Deploy Monitoring Helm chart
        env:
          KUBE_NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}
        run: |
          helm upgrade --install monitoring-helmcharts ./monitoring-helmcharts \
            --namespace $KUBE_NAMESPACE \
            --set serviceName=${{ steps.extract-values.outputs.service_name }} \
            --set namespace=$KUBE_NAMESPACE \
            --set monitoring.interval="15s" \
            --set alertLabels.severity="victims-pathfinder-test"
