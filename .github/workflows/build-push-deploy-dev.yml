name: CI/CD dev

on:
  workflow_dispatch:

permissions: {}
concurrency:
  group: dev
  cancel-in-progress: false  # Optional — don’t cancel ongoing runs, just queue new ones

jobs:

  backup-vars-test:
    name: Backup variables
    runs-on: ubuntu-latest
    environment: test
    steps:
      - name: Authenticate to the cluster
        env:
          KUBE_NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}
          KUBE_CLUSTER: ${{ secrets.KUBE_CLUSTER }}
        run: |
          echo "${{ secrets.KUBE_CERT }}" > ca.crt
          kubectl config set-cluster ${KUBE_CLUSTER} --certificate-authority=./ca.crt --server=https://${KUBE_CLUSTER}
          kubectl config set-credentials deploy-user --token=${{ secrets.KUBE_TOKEN }}
          kubectl config set-context ${KUBE_CLUSTER} --cluster=${KUBE_CLUSTER} --user=deploy-user --namespace=${{ secrets.KUBE_NAMESPACE }}
          kubectl config use-context ${KUBE_CLUSTER}

      - name: Backup variables
        env:
          KUBE_NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}
          SECRETS_CONTEXT:  ${{ toJson(secrets) }}
          VARS_CONTEXT:  ${{ toJson(vars) }}
        run: |

          gh_vars_json=$(jq -n \
              --argjson vars_json "$VARS_CONTEXT" \
              '$vars_json')

          gh_secrets_json=$(jq -n \
              --argjson secrets_json "$SECRETS_CONTEXT" \
              '$secrets_json')

          echo "$gh_vars_json" > "github-variables-test"
          echo "$gh_secrets_json" > "github-secrets-test"


          echo "Creating configmap for environment variables "
          kubectl create configmap github-environment-variables-test-11march --from-file=github-variables-test -n $KUBE_NAMESPACE

          echo "Creating secret for secret variables "
          kubectl create secret generic github-secret-variables-test-11march --from-file=github-secrets-test -n $KUBE_NAMESPACE

          echo "cleaning up"
          rm -f github-variables-test github-secrets-test